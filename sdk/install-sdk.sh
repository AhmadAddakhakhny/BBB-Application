# TBD:
#=====

# This script makes it easy to install or update the SDK locally without committing binaries.
# You can optionally publish the SDK .sh file via GitHub Releases, so your teammate can download it with curl or wget.
